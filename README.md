# 채록 (Chaerok) · Roll20 로그 백업 도우미

Roll20 Chat Archive를 붙여넣기만 하면 정리해서 **티스토리에 바로 올릴 수 있는 HTML**과
**깔끔한 PDF**로 만들어주는 정적 웹앱입니다. 서버나 빌드 과정 없이 순수 HTML/CSS/JS로
동작하며, GitHub Pages로 바로 배포할 수 있습니다.

## 왜 다시 만들었나

기존 버전(`dedvil.github.io/roll20-log-editor`)은 로그 입력창이 `<textarea>`였기 때문에
Roll20 Chat Archive에서 복사한 서식 있는 HTML을 붙여넣어도 **일반 텍스트로만 저장되어
화자·서식·이미지 정보가 모두 사라지는 문제**가 있었습니다. 이번 버전은 입력창을
`contenteditable`로 바꾸고, Roll20의 실제 채팅 마크업(`.message`, `.by`, `.whisper`,
`.rollresult` 등)을 인식하는 파서를 새로 작성해 이 문제를 해결했습니다.

## 주요 기능

- **붙여넣기 한 번으로 인식** — Chat Archive를 통째로 복사해 붙여넣으면 즉시 자동 파싱.
  **정리(숨김 삭제·중복 제거·부호 교정)는 "정리 실행" 버튼을 눌러야만 적용됩니다** —
  붙여넣자마자 원치 않게 정리가 실행되지 않습니다.
- **Roll20 실제 채팅창과 동일한 그룹핑** — 같은 화자가 연속으로 말하면 아바타·이름은
  한 번만 표시하고, 그 아래로 대사(말풍선)만 여러 줄 쌓입니다. 숨김 메시지가 삭제되면
  해당 줄만 사라지고(아바타·이름은 남은 대사가 있으면 유지, 그 화자의 메시지가 그것
  하나뿐이었다면 아바타·이름까지 함께 사라짐), 중간에 나레이션이 끼어들면 그룹이
  끊겨서 대사 말풍선에 함께 묶이지 않습니다.
- **말풍선은 대사에만** — "말풍선(카드) 표시"를 켜면 아바타·이름을 제외한 대사 텍스트
  부분에만 실제 말풍선 모양(카카오톡 스타일, 아바타 쪽 모서리만 각진 모양)이 적용됩니다.
- **캐릭터 아바타 자동 인식** — Roll20 메시지에 포함된 아바타 이미지를 자동으로 찾아 표시하고,
  원본이 만료되어 깨진 경우 클릭 한 번으로 이미지를 직접 올려 교체할 수 있음
- **내레이션(지문) 자동 구분** — 화자 없는 GM 지문/설명은 가운데 정렬로, 캐릭터 대사는
  아바타+이름과 함께 왼쪽 정렬로 자동 구분. 캐릭터별 "역할"을 캐릭터/나레이션으로 직접
  바꿔서 강제로 정렬 방식을 지정할 수도 있음
- **판정(주사위) 결과 자동 색칠** — 커스텀 시트의 "성공/실패/어려운 성공/극단적 성공" 등
  결과 텍스트를 인식해 다른 매크로·CSS 없이도 기본적으로 초록/빨강 등으로 색이 들어갑니다.
  Roll20 기본 크리티컬/펌블(`.fullcrit`/`.fullfail`) 강조도 함께 복원됩니다.
- **깜빡이는 안내문·재생 아이콘 중복 정리** — "목목적적지지..." 처럼 애니메이션 잔재로
  글자가 겹쳐 보이거나 `♬♬♬ ♪♪♪ ▶▶▶■■■` 처럼 아이콘이 여러 번 겹쳐 찍히는 경우를
  자동으로 감지해 정리합니다.
- **저장한 HTML + 이미지 폴더 업로드** — "웹페이지, 전부"로 저장한 경우도 지원
- **로그 두배 렌더링 오류 자동 해결**
- **배경색 자동 통일** — 대사류 메시지의 제각각인 배경색만 통일(판정 결과색은 보존)
- **노래/링크 자동 변환** — `[♬ 링크](url)` 같은 마크다운 스타일 텍스트를 실제 링크로 변환
- **캐릭터 관리** — 이름 클릭으로 이름변경·병합, 선택 캐릭터 로그 일괄 삭제
- **상세 통계** — 전체·캐릭터별 메시지 수, 글자수(공백 포함/제외), 타입별 집계
- **검색 · 타입 필터** — 대사/감정/귓속말/판정/내레이션/시스템별로 필터링
- **본문 바로 편집** — 문장을 클릭하면 바로 수정, ⠿ 드래그로 순서 변경, 메시지 삭제, 실행 취소
- **읽기 설정** — 스킨(다크/라이트/세피아/민트, 기본값은 밝은 라이트), 캐릭터·나레이션 폰트,
  글자 크기/자간/줄 간격/문단 간격, 아바타 표시, 말풍선(카드) 표시 등을 자유롭게 조정 —
  미리보기와 내보내기 결과에 동일하게 반영됨
- **내보내기**
  - 티스토리 HTML 클립보드 복사 / 파일 다운로드
  - PDF 저장 (브라우저 인쇄 다이얼로그, 표지 페이지 지원)
  - TXT / JSON 백업 다운로드 및 JSON 복원 (아바타 교체, 역할 지정 등 모든 설정 포함)
- **자동 저장** — 브라우저 `localStorage`에 자동 저장되어 실수로 탭을 닫아도 복구 가능
- **스크롤해도 사라지지 않는 상단바** — 페이지 자체는 스크롤하지 않고 왼쪽 패널/로그
  영역만 각자 내부 스크롤하는 구조라, 아무리 로그가 길어도 상단바·툴바가 항상 보입니다
  (좁은 화면(모바일)에서는 평범한 한 화면 스크롤 + 상단바만 sticky로 전환됩니다).

## 화면 구성

- **왼쪽 사이드바**: 로그 가져오기, 자동 정리, 캐릭터 매핑(아바타/역할/이름변경)
- **가운데 메인**: 검색·필터 툴바 + 로그 미리보기(=본문 편집 영역)
- **읽기 설정 드로어**: 우측 상단 "⚙ 읽기 설정" 버튼으로 열리는 슬라이드 패널
- **내보내기 드로어**: 우측 상단 "내보내기" 버튼으로 열리는 슬라이드 패널(통계·색상·표지·파일 내보내기)


## 폴더 구조

```
index.html
css/
  style.css      앱 UI 스타일
  print.css      PDF(인쇄) 전용 스타일
js/
  utils.js       공용 헬퍼
  parser.js      Roll20 HTML / 텍스트 로그 파서 (판정 색칠, 글자/아이콘 중복 정리 포함)
  cleaner.js     자동 정리(부호 교정, 중복 제거, 숨김 삭제) — "정리 실행" 버튼으로만 동작
  characters.js  캐릭터 추출·이름변경/병합·일괄삭제
  stats.js       통계 계산
  search.js      검색/하이라이트
  grouping.js    연속 발화 그룹핑 규칙 (미리보기·내보내기 공용)
  renderer.js    화면 렌더링
  exporter.js    티스토리 HTML / PDF / TXT / JSON 내보내기
  app.js         상태 관리 및 이벤트 와이어링
```

빌드 도구나 npm 의존성이 없습니다. `index.html`을 브라우저로 열거나 정적 파일 서버로
서빙하면 그대로 동작합니다.

## 로컬에서 열어보기

```bash
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

(파일을 더블클릭해 `file://`로 직접 열어도 대부분의 기능이 동작하지만, 클립보드 복사
등 일부 브라우저 API는 `http(s)://`에서 더 안정적으로 동작합니다.)

## GitHub Pages로 배포하기

1. 이 폴더의 내용을 저장소(`roll20-log-editor` 등) 루트에 그대로 올립니다.
2. GitHub 저장소의 **Settings → Pages**에서 배포 브랜치를 지정합니다.
3. `https://<사용자명>.github.io/<저장소명>/` 주소로 접속해 확인합니다.

## Roll20 채팅 로그 가져오는 방법

앱 상단의 **"가져오기 방법"** 버튼에서도 동일한 안내를 볼 수 있습니다.

1. Roll20 캠페인 페이지 → **[이 게임의 모든 채팅 내용 보기]** (또는 **[게임 > 내용 > 채팅 기록]**)
   → Chat Archive 화면에서 **[Show on One Page]** 클릭
2. **방법 1 (추천)**: 전체 선택(Ctrl/Cmd+A) → 복사(Ctrl/Cmd+C) → 이 앱의 붙여넣기 상자에
   붙여넣기(Ctrl/Cmd+V)
3. **방법 2 (이미지까지 보존)**: 브라우저의 "페이지를 다른 이름으로 저장 → 웹페이지, 전부"로
   저장한 뒤, 생성된 `.html` 파일과 `_files` 폴더를 이 앱에 업로드

